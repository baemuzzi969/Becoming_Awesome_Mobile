import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Becoming_Awesome_Mobile',
  webDir: 'dist'
};

export default config;
